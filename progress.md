# Progress

- Planning completed and explicitly approved for implementation.
- Baseline tests: 25 passed. Working tree initially clean; main tracks origin/main at 0001160.
- Implementation begun; skills read; no live credentials printed or API translation requested.
- Git push for rollback tag failed (github.com:443 connection timeout). Using authenticated GitHub refs API for the same baseline tag; Git transport will be retried for commits.
- Rollback tag successfully created remotely at baseline via GitHub API.
- Stage 1: per-user DPAPI settings, provider isolation, compact conditional panels, frozen worker input snapshots and queue-delivered errors. Seven targeted tests passed, including real Windows DPAPI round trip.
- One patch application rejected a delete/add of the same file; split into sequential operations, no partial patch applied.
- Stage 2: official v4 upload/poll/download, saved part states, lossless page splitting, measured byte limits, chapter hints, seam windows, namespaced assets and conservative merge are implemented. 47 tests pass. Synthetic 199/200/201-page files preserve page dimensions, rotation and text; byte-limit and resume scenarios pass.
- Stages 1 and 2 committed as 2d27364 and 39b8f57 and pushed to codex/mineru-quality-reliability; Git HTTP/1.1 transport succeeded.
- Stage 3 implemented: document blocks, reference boundaries, per-block integrity checks, targeted paragraph/cell revisions, validated persistent caches, source/config fingerprints, bounded workers and task resume. DOCX now preserves rowspan/colspan and inline-image surrounding text; failed exports produce review items.
- Validation: 80 tests pass on Windows/Python 3.14. Added Windows Python 3.11/3.14 CI. Credentials absent from source/docs scan; outputs and credentials ignored by Git.
- Synthetic academic live sample completed through existing DeepSeek configuration. Initial three requests took about 81.4 seconds; one unnecessary revision exposed CJK-adjacent numeric false positives. Fixed and regression-tested. Revalidated real cached responses with network disabled: completed, no warnings, zero new requests.
- Default DOCX renderer could not run (system Python lacked pdf2image; bundled runtime had it but no LibreOffice). Used an isolated invisible Word COM instance to export DOCX to PDF, then Poppler to render; manually inspected the whole page, tables, superscripts/subscripts, figure/caption and post-reference chapters.
- GUI API view inspected using a temporary settings store and empty keys. No real MinerU token is configured, so official protocol behavior is mock-tested and live OCR is explicitly unverified.
- GitHub Python 3.11 CI exposed cp1252 redirected-stdout failure when printing Chinese CLI report paths; fixed CLI stream encoding to UTF-8 and reproduced with an explicit cp1252 test. Local 80 tests pass; rerunning both remote versions.
- GitHub run 34300187632 passed all 80 tests on both Windows Python 3.11 and 3.14. Fast-forwarded main to 8902d98 and pushed successfully; ls-remote confirmed main=8902d9879934a4300f948396833d284110a4292b and rollback tag=000116058b053f3016dfb57388b4aa34c1d360a2. Remaining publication is this documentation-only acceptance record.
- Follow-up reproduced old behavior with 71 live requests; fixed run reused completed responses and needed 4 new requests to finish 8/8. Subsequent resume with model calls forbidden passed. 88 regression tests pass; real MinerU task query returned done and original PDF hash matched. Remaining review items are OCR/math commands and preserved English names.
- 2026-09-09: Began a separate local MinerU GPU deployment. Existing planning records were preserved and extended. Deployment will be checkpointed so interruption can safely resume from inspected local state.
- Research checkpoint: reviewed current official quick-start, release list, Windows CUDA FAQ, official changelog, and the supplied CNBlogs tutorial. Stable 3.4.5 is preferred over MinerU 4.0 alpha; exact GPU family must be checked before choosing CUDA dependencies.
- Preflight checkpoint: RTX 5070 Ti Laptop (Blackwell, 12 GB), driver 581.57/CUDA capability 13.0, Windows 11, 31.5 GB RAM, 132 GB free on D:, Conda 26.1.1 at the requested location. `D:\Application\MinerU-Local` is present and empty. Selected architecture: Python 3.12 prefix environment under that root, official Blackwell lmdeploy cu128 route, isolated model/config paths under the same root.
- Environment checkpoint: created Conda prefix `D:\Application\MinerU-Local\env` with Python 3.12.14 and pip 26.2.1. Conda encountered one transient defaults-channel disconnect while reading an unused channel, retried automatically, and completed successfully with a verified transaction. Existing Conda environments were untouched.
- Installed uv 0.12.11 in the new environment. A diagnostic attempted to read `uv.__version__` and failed because uv does not expose that Python attribute; the supported CLI and package metadata checks both report 0.12.11, so no installation fault exists.
- Dependency dry-run attempt 1 did not mutate the environment and failed because uv gives its extra PyTorch index first-index priority: that index exposes only `tqdm<=4.66.5`, while MinerU requires >=4.67.1. Root cause is index selection, not MinerU compatibility. Will keep the normal PyPI resolver separate from the official Blackwell wheel/PyTorch step instead of weakening dependency-confusion protections globally.
- Dependency dry-run attempt 2 used pip's normal primary/extra-index semantics and solved MinerU 3.4.5 plus the official lmdeploy 0.11.1+cu128 wheel together. The compatible GPU stack is torch 2.8.0+cu128, torchvision 0.23.0+cu128, transformers 4.57.6, and OpenAI Python 2.54.0. No package changes were made during either dry-run.
- During the large CUDA install, a read-only network-rate diagnostic initially had an invalid PowerShell pipeline after a `foreach` block. Root cause was missing assignment/parentheses; the corrected diagnostic measured about 55 Mbps and did not affect installation.
- Resume checkpoint after usage-limit interruption: the original installer process no longer exists. The environment is internally consistent and contains torch 2.8.0+cu128 (but not yet torchvision, lmdeploy, or MinerU), occupying 7.70 GB. CUDA verification already passes on the RTX 5070 Ti: runtime 12.8, `torch.cuda.is_available() == True`, compute capability `(12, 0)`. Remaining installation can safely reuse this state/cache without recreating the environment.
- Resumed the same compatible pip solve. It reused the completed CUDA stack and successfully installed MinerU 3.4.5, torchvision 0.23.0+cu128, lmdeploy 0.11.1, and all Windows/all extras. `pip check` reports no broken requirements; direct imports, CLI version/help, and CUDA device checks all pass.
- A read-only ModelScope cache-path probe called `get_model_cache_dir()` without its required `model_id`; root cause is an incorrect diagnostic invocation, not a deployment issue. Installed source confirms `MODELSCOPE_CACHE` is supported, and MinerU source confirms absolute `MINERU_TOOLS_CONFIG_JSON` is supported. The config parent directory must be created before download because MinerU writes the file directly.
- Created isolated config/model/cache/output/log/sample directories under `D:\Application\MinerU-Local` and stored environment-scoped paths in the Conda prefix (`MODELSCOPE_CACHE`, `HF_HOME`, `MINERU_TOOLS_CONFIG_JSON`, `PIP_CACHE_DIR`, `UV_CACHE_DIR`). The ModelScope resolver confirms the resulting model cache is under the requested root.
- Initial model-download launch through `conda run` appeared stuck because stdout was buffered and ModelScope's incomplete placeholder stayed at logical length zero; after an exact-process interruption, a direct resumable launch revealed 193/215 MB already cached and completed the first model. Root cause was misleading observability rather than lost data.
- Interruption also exposed an upstream resume weakness: once MinerU writes a configured pipeline root, a partially created subdirectory can be treated as already present and skipped. The 810 MB MFR submodel is therefore scheduled for an explicit ModelScope snapshot integrity repair after the main download, followed by a full `.incomplete` scan.
- ModelScope Hub 0.4.1 source shows its large-file range downloader defaults to one stream despite supporting up to 16 via `MODELSCOPE_DOWNLOAD_PARALLEL_WORKERS`. Set an environment-scoped value of 8 for files above the default 500 MB threshold; smaller files continue with the library's normal file-level concurrency.
- Model transfer recovery was hardened with `D:\Application\MinerU-Local\repair_model_cache.py`, which checkpoints every submodel, uses the installed MinerU 3.4.5 model manifest, resumes official Hub cache downloads, rejects unresolved `.incomplete` files, and writes `model-download-state.json`. An idempotent `Resume-Install.ps1` now recreates only missing pieces and can be rerun after quota, shell, or network interruption.
- ModelScope range concurrency performed poorly through the current proxy, while Hugging Face Xet was reliable for individual large files and ordinary resumable HTTP was faster for many small OCR files. The final policy uses regular HTTP with four workers for pipeline assets and Xet for the single large VLM weight.
- Pipeline model set completed and `mineru.json` was written under the requested root. Verified downloads include layout, UniMERNet/MFR, all Paddle OCR language/detection weights, table models, and the 617 MB PP-FormulaNet model. VLM download is now in progress and remains resumable.
- Created `Parse-Document.ps1` and `Start-WebUI.ps1`; all three user-facing/recovery PowerShell scripts parse with zero syntax errors. A read-only storage diagnostic initially repeated the known PowerShell `foreach`-to-pipeline parser pitfall; assigning the loop result before sorting fixed it, with no deployment mutation.
- VLM snapshot `opendatalab/MinerU2.5-Pro-2605-1.2B` completed (2.31 GB main tensor) and MinerU wrote both immutable snapshot paths to the isolated config. Post-download validation: valid JSON, both model roots exist, zero `.incomplete` files, checkpoint status `complete`, and `MINERU_MODEL_SOURCE=local` is persisted in the Conda prefix.
- `lmdeploy check_env` passes: Windows/Python 3.12.14, RTX 5070 Ti, CUDA available, PyTorch 2.8.0+cu128 with explicit `sm_120`, torchvision 0.23.0+cu128, lmdeploy 0.11.1. The system toolkit remains CUDA 12.4, while the installed wheels correctly use their CUDA 12.8 runtime.
- Representative two-page PDF smoke test passed through `hybrid-engine` medium. Runtime logs explicitly selected `lmdeploy-engine`, `device=cuda`, `turbomind`, and a 12 GB-aware batch ratio. Output contains nonempty Markdown (1,348 characters), both content-list JSON variants, middle/model JSON, layout/origin PDFs, and extracted image assets.
- Web UI launch test passed on `127.0.0.1:7860` with HTTP 200 and MinerU content, then was cleanly stopped. A full second run of `Resume-Install.ps1` completed in about 16 seconds, reused every cached package/model, found no dependency faults, and proved idempotent recovery after completion.
- Final user entry points are `Start-WebUI.cmd`, `Parse-Document.cmd` (drag-and-drop), and `Resume-Install.cmd`, backed by validated PowerShell scripts. `README-本地使用说明.md` records paths, usage, backend choices, compatibility reasoning, recovery, and upgrade cautions.
- Regression tests first reproduced both failures, then all 90 tests passed after the scoped fix.
- Live discovery also found an older MinerU 3.1.12 in ai_book. Selected the previously deployed 3.4.5 explicitly in current-user GUI settings (encrypted settings backup preserved), instead of relying on ambiguous registry ordering.
- Final local acceptance: MinerU 3.4.5 detected through the configured path; GUI-equivalent cancellable subprocess path parsed the existing two-page synthetic academic PDF successfully with backend auto (installed default hybrid-engine). Markdown contains headings, text, merged table and references.
- Reopened hidden GUI restored the selected executable. Cache resume succeeded with subprocess launch forbidden. No translation API calls were used in this acceptance.
- Full regression: 90 tests pass; git diff --check passes. README documents CLI discovery/configuration. Local settings backup: %LOCALAPPDATA%\TranslationPipeline\settings.before-mineru-20260915.json.
- Project recommendations remain unimplemented; code changes only address CLI discovery, selected environment propagation, text encoding and detection diagnostics.
- Stage 1: implemented live UTF-8 log observation, page progress, task-owned process-tree shutdown, metadata/model-aware cache checks, parser lock, fresh attempt directories and original-source records.
- Focused regression found a test locking the output parent instead of the actual job directory; corrected test path (implementation lock behavior passes).
- Stage 2: opt-in global/book glossaries, local indexed longest-phrase matching, CSV conflict resolution, bounded explicit extraction, management window, CLI options and per-segment/per-cell effective-term cache keys implemented.
- Seven focused glossary tests pass, including 10,000 unrelated terms producing identical prompts and only affected segments re-requested. Existing 95 regression tests passed before the new glossary cases.
- Stage 3: durable block/cell alignment, conservative source-page matching, reversible manual edits, targeted candidate translation, offline recovery, export retry and bilingual desktop review implemented; 11 focused review tests pass.
- CI exposed Windows short-path versus resolved long-path differences in local parser returns and a test assertion. Normalized parser output roots and made the PATH test compare resolved paths. No parser cache corruption or environment configuration failure was involved.
- Local Python 3.11 lacked dependencies; created a separate ignored validation environment under tests/.artifacts and installed only project requirements.
- Acceptance: 117 tests pass locally on Windows Python 3.11 and 3.14, including GUI geometry/read-only checks and actual Windows descendant-process cancellation. CI for stage 3 passed both versions (run 34960684212).
- Live local MinerU 3.4.5: two-page parse succeeded with continuous activity/page events; no-process cache resume succeeded.
- Live DeepSeek synthetic sample: disabled/enabled both completed in 2 requests, 0 retries; input tokens 1060/1106; wall times 17.12/18.29 s. No claim of general cost savings.
- Live explicit extraction exposed whole-sentence candidates yielding no usable terms. Added local stop-word/punctuation bounded n-grams; revalidation produced technical candidates including oxygen concentration and recovery period. Suggestions remain opt-in and require selection.
- Live targeted paragraph proposal/adoption and no-model export retry succeeded. Main/glossary/review windows visually checked using window capture; fixed cropped review controls and formula-related false review warnings. Word/PDF export visually checked.
- Initial screen-region captures were black in this desktop state; window-specific capture worked. Optional win32gui probe was unavailable; Pillow's existing window capture avoided installing another dependency.
- Final feature code 1c68306 passed GitHub Windows Python 3.11 and 3.14, 117 tests each (run 34961700245). Release documentation records exact live metrics, known limitations and rollback commands.
- Published and verified: main and release/high-priority-20260915 both resolve remotely to 8c88eff46e443a38b26de88a01a4ecb4ab72a94b; rollback/pre-high-priority-20260915 resolves to 517df3e95e2dd27429223c4a95c7a16480fdfb5a. Working tree clean at publication. This final audit update changes planning records only.
- 2026-09-16 backend follow-up: the GUI hardcoded only auto/pipeline although installed MinerU 3.4.5 supports hybrid-engine and vlm-engine. Added both local choices and explanatory text; HTTP client choices require a separate inference-server URL and remain outside this local form. Auto still omits -b and uses the installed default (hybrid-engine in 3.4.5).
- Verified the selected MinerU environment reports torch 2.8.0+cu128, CUDA available, RTX 5070 Ti Laptop GPU. Previous successful two-page parsing log explicitly records CUDA/TurboMind. Extended backend forwarding regression; all 117 tests pass on Windows Python 3.11 and 3.14. Inspected the local-mode advanced panel screenshot; controls and explanation fit. Existing release/rollback tags remain intact.

- 2026-09-16: baseline rollback/pre-layout-review-20260916 pushed; output migration, local located reports, conservative omission candidates and export layout implementation in progress.
- Regression diagnosis: legacy path assertions needed migration to artifact locator; a scripted replacement accidentally produced self.artifact and was corrected. Main-thread Tk cleanup prevents test objects being finalized in worker threads. Latest 117-test run completed with one remaining old _previous path assertion, now updated. Python 3.11 validation environment has no pip; installed new renderer dependencies using uv pip with its explicit interpreter.

- Final local regression: 131 tests pass on Windows Python 3.11 (25.839s) and 3.14 (21.742s). User task migrated with a complete legacy snapshot; reexport guarded against all model calls.
- Visual QA: native Word PDF export, all-page contact sheets, bilingual review window. Corrected chained figure captions, width handling for 13-column table, redundant nested formula font groups and source chapter attribution. Result 28 pages; 27 review issues include 5 unadopted omission candidates.
- Staged pushes: 86dbff2 storage/report foundation; 70a7d04 conservative parser metadata; f60b651 integrated export/review workflow. Final acceptance commit pending.

- Final concurrency audit: settings change and reexport now share one lock; prior layout settings enter history first. Added GUI source/translation/ID search. Preparation upgrades reuse only exact hash-validated original segment snapshots; real task copy resumed all 8 segments with zero model calls.
- Updated complete suite: 133 tests pass locally on Windows Python 3.11 (27.119s) and 3.14 (23.215s). GitHub passed f25d9dc on both versions before the final preparation regression addition.

- Final feature ddfffd5 passed GitHub Windows 3.11/3.14 (133 tests each), run 35050203134. All implementation and local/remote acceptance complete. Publishing documentation-only audit with release/output-review-layout-20260916; baseline rollback remains d4a5801.
